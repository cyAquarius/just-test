# just-test

- 默认使用中文协作，代码标识符和命令保持英文。
- 本仓库是可复用的测试工具库，不得引入特定业务项目的包、依赖、表结构或用例。
- 框架代码统一放在 `com.just.test.smarttest` 下。
- 仅用于演示的代码放在 `src/test` 的 `com.just.test.smarttest.demo` 下。
- 除非明确批准迁移，否则必须保持 Java 8 兼容性。
- Java 构建和测试直接使用 Maven。
