# just-test

just-test is an AI-oriented testing toolkit alongside just-coding and just-review.

Its first framework is SmartTest, a Spring, H2 and YAML data-driven integration testing framework.

## Package

com.just.test.smarttest

## Lifecycle

clean -> prepare.yaml -> BeforeCase -> test -> result, exception and database verification -> clean

The repository contains reusable framework code and one minimal demo. It contains no migrated application business code or business test cases.
